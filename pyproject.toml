[project]
name = "goit-ds-hw-01"
version = "0.1.0"
description = "Personal assistant project"
authors = [
    {name = "Mozolik", email = "Mozollik@gmail.com"}
]
requires-python = ">=3.12"
dependencies = []

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"